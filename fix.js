const fs = require("fs"); const file = "src/pages/Sustainability.tsx"; let content = fs.readFileSync(file, "utf8"); content = content.replace(/Drop,/g, "Droplets,"); if (!content.includes("Check")) { content = content.replace(/AlertCircle\n}/g, "AlertCircle,\n  Check\n}"); } content = content.replace(/indicatorClassName="bg-milgen-yellow"/g, "className=\"h-2 bg-gray-100\""); fs.writeFileSync(file, content); console.log("File fixed successfully!");
